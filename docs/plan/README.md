# Claude Code Multi-Session Manager — Implementation Plan

A Spring Boot backend + React dashboard for running parallel Claude Code sessions,
each in its own Git worktree, driven through Node sidecars using the Claude Agent SDK.

## Decision log

| Date | Decision |
|---|---|
| 2026-08-23 | Integration via **Agent SDK sidecar** (one Node process per session), not raw CLI control protocol |
| 2026-08-23 | Deployment: **LAN, single user** → bearer-token auth on REST + WS, optional TLS, configurable bind address |
| 2026-08-23 | Persistence: **PostgreSQL** (sessions, event journal, config templates; pgvector-ready for later RAG/memory) |
| 2026-08-23 | Frontend: **React + react-grid-layout**, Vite, TypeScript |
| 2026-08-23 | **Java 25** (matches existing pom; IntelliJ manages the JDK, CLI builds need it on `JAVA_HOME` too) |
| 2026-08-23 | **Flyway** manages all Postgres schema migrations (versioned `V*__*.sql`, applied on backend startup) |
| 2026-08-23 | WS auth token is carried in the **`Sec-WebSocket-Protocol` header**, never the query string |
| 2026-08-23 | Primary deployment target is a **Mac laptop**; WSL is only the current dev box. No filesystem-placement constraints for worktrees — `worktree-root` is just a config value (default `~/claude-worktrees`) |
| 2026-08-23 | **Multi-service ecosystem context**: session config carries **two folders** — the specific service repo (checked out as the writable worktree) and its **parent ecosystem folder** containing the sibling services, attached **read-only** (SDK `additionalDirectories` / `--add-dir`; writes into it auto-denied by the sidecar) so Claude can read how the service's peers communicate |
| 2026-08-23 | **Per-session service & ecosystem selection** (pulled forward from backlog 5.7): the create dialog offers every git repo under `ecosystem-root` as the session's service (worktree source, `repoPath` in the create request), and the ecosystem context path is an editable per-session field (empty = no wider context). `claude-ui.repo-path` remains only the default selection |
| 2026-08-23 | **Provider-agnostic backend & UI**: the sidecar NDJSON contract is the *provider adapter interface* — the Claude sidecar is its reference implementation. Neutral naming (`providerSessionId`), a **capabilities handshake** in `ready` drives which controls the UI renders, sessions carry a `provider` id (default `claude`) + opaque `providerConfig`, adapters are launched from per-provider config, and asset materialization is a per-provider strategy. No second adapter until Phase 5 (Codex CLI candidate); the Claude UX is never dumbed down — capabilities gate the controls |
| 2026-08-23 | **Per-session Claude behavior config**: thinking budget preset, max turns per prompt, cost budget, extra system instructions, fallback model, custom subagents (materialized like skills), template kickoff prompts with placeholders. **Auto-edits vs per-edit confirmation = `permission-mode`** (`default` asks, `acceptEdits` auto-applies), togglable **mid-session** from the widget header via a `set_permission_mode` sidecar command. Message queueing while RUNNING, auto-titling, and idle-session parking included; hooks injection, transcript export, checkpoints/rewind, prompt fan-out, usage dashboard → Phase 5 |
| 2026-08-23 | **Skills per session**: session/template config lists *skill sources* — a skill dir, a single `SKILL.md`, an index file listing skill paths, or a **git repo of skills** (URL; cloned/updated into a local cache, skills discovered inside). The backend materializes them into the worktree's `.claude/skills/` at provisioning (symlink; copy fallback), never clobbering the repo's own skills; the sidecar loads project settings so they're discovered |
| 2026-08-30 | **Codex CLI provider adapter (5.13)**: targets `codex app-server`'s JSON-RPC-over-stdio protocol (not `codex exec`, which is non-interactive), in a new sibling `sidecar-codex/` package — `provider` was already a wired-through per-session field with no adapter to select. First pass is an MVP (no skills/MCP passthrough/plan mode); cost is estimated from Codex's token counts against a **Settings-editable per-model price table** (`codex.pricing`), since Codex reports no per-turn USD. Provider is chosen **per session/template**, defaulting from a new **Settings** entry (`session.default-provider`). Full mapping tables and rationale in [phase-5.13-codex-provider.md](phase-5.13-codex-provider.md) |
| 2026-08-30 | **Codex follow-up**: skills and MCP flipped to `skills: true`/`mcp: true` after live confirmation both are feasible — skills via `skills/extraRoots/set` pointing at the already-materialized `.claude/skills/` (Codex reads the same `SKILL.md` format), MCP via `thread/start.config.mcp_servers` (thread-scoped, not the global `codex mcp add` registration), with bearer tokens passed as a named env var on the spawned child rather than an inline header. **Agents confirmed permanently infeasible** (no Codex equivalent to Claude's static subagent files exists at all — `agentSources` is now rejected at creation for `provider: codex`, not silently dropped). `acceptEdits`/`plan` permission modes re-investigated and confirmed still not mappable — stays at 2 modes. The create dialog's permission-mode control changed from a `<select>` to a row of clickable chips (capability-filtered, same component for every provider) at the same time. See the design doc's "Follow-up" section for the full research. |
| 2026-09-05 | **Layered memory & reflection (5.3, redesigned)**: the original per-turn `memory_chunk` RAG sketch is superseded. Per-session **reflection switch** — one structured system-session turn at close (+ manual "Reflect now") retrospects the transcript and emits JSON memory ops. Memory is **layered**: episodic (`memory_episode` DB rows, append-only, auto-injected as a recent same-service window at spawn) vs semantic (**Markdown files with YAML frontmatter** as source of truth in a managed `memory.root`, DB rows only an index — human-editable, hash-synced), scoped **ecosystem vs service** (keyed by `repo_path`). Retrieval is **hybrid**: pgvector dense + Postgres FTS/`pg_trgm` sparse, fused with RRF (no ParadeDB — stock image). Agents consume memory via **hierarchical paging tools** (`memory_tags`/`memory_search`/`memory_read`) on an injected stdio MCP server (`memory-mcp/`, provider-neutral, works on Codex too) — no static top-k injection. Semantic memories interlink with **Obsidian-style `[[wikilinks]]`** (the memory root is a valid Obsidian vault; dangling links allowed and auto-resolved), indexed into `memory_link` so reads return the one-hop related-memory neighborhood with descriptions. Journal retention policy lands here, default keep-forever. Full design in [phase-5.3-memory-reflection.md](phase-5.3-memory-reflection.md) |
| 2026-09-06 | **Phase 7 planned (UX & orchestration)**: 7.1 single-key Gmail-style hotkeys over a new focused-widget concept (`y`/`d` approve/deny is the hot path); 7.2 window management keeps tiling as the base — maximize is CSS-only on the live grid item (no remount, WS intact), minimized widgets stay **mounted but hidden** (notifications + store keep working), Exposé renders **live store-backed cards** (the store already holds every transcript; zero extra WS). 7.3 continuation transfers context as an **AI handoff summary by default** (system-turn over `TranscriptDigest`, /compact pattern; raw-digest checkbox for fidelity), landing as an editable unsent draft (ticket-import precedent); retention-pruned sessions are not continuable. 7.4 multi-service fan-out via **orchestration MCP tools on the existing in-process server** — spawn is human-gated by the *normal tool-permission prompt* (requires narrowing the blanket `mcp__memory` pre-approval to the three read-only tools), children **push** results (`report_result` → parent queue, waking a PARKED parent) and **stay open for review**; depth 1 only. V11 adds `continued_from_id` + `parent_session_id`. [phase-7-ux-and-orchestration.md](phase-7-ux-and-orchestration.md) |
| 2026-09-06 | **Phase 8 planned (ecosystem service discovery)**: per-service description + embedding, DB-only cache (`service_profile`, V12), regenerated at session close when missing/stale (2-week default) or on a manual "Rediscover"/"Scan ecosystem now" action — no scheduled sweep, no approval gate (unlike 5.3's memory, this is a disposable derived summary, not curated durable fact). Two new tools, `service_description`/`find_service`, land on the **same shared in-process MCP server** 5.3 stood up (`/api/mcp/memory`); its attachment condition generalizes to `memory.enabled || service-discovery.enabled`, with each tool set self-gating on its own setting so one feature being off holds even when the other keeps the server attached. Scoped to the calling session's own `ecosystemPath`, same as 7.4's `list_services`. [phase-8-service-discovery.md](phase-8-service-discovery.md) |
| 2026-09-06 | **Phase 9 planned (architectural review & production hardening)**: a full-system review produced a curated backlog instead of one feature plan — test coverage first (the system has a single `contextLoads` test; pure-logic JUnit + a protocol-sync guard + minimal CI before any refactor), then de-Claude-ing the backend's LLM plumbing (system session hardcodes provider `claude`/model `haiku`, auto-title shells out to raw `claude -p` — both break a Codex-only install; fix via per-provider model catalog in the capabilities handshake), dedup (`stripFences`/`truncate` ×5 → one `SystemTurnClient`; `toVectorLiteral` ×4; MCP-layering merge ×2), and structural simplification (`SessionService` at 1,164 lines splits along system-session/config-factory/housekeeping seams; `SessionEntity`'s ~40-arg constructor → builder). Docs drift catalogued (DEPLOY.md five phases stale, PROTOCOL.md missing 3 event types, ARCHITECTURE.md missing Phase 8). Security stays out of scope (LAN/single-user posture). Items are picked per run, not all at once. [phase-9-production-hardening.md](phase-9-production-hardening.md) |
| 2026-09-07 | **Phase 11 planned (monorepo support)** — extracted from phase 10's §10.5 into its own doc and split into eight steps with a DoD each: a Step 0 spike (does Claude Code load `.claude/skills` from a subfolder cwd or only the git root's — decides where provisioning materializes assets), then `ServiceDetector` + `findServices` (manifest-driven: npm/yarn/pnpm/Maven/Gradle/Cargo/go.work, glob fallback via a new `ecosystem.monorepo-service-globs` setting), V14 (`session.service_path`, `service_profile.repo_path` → `service_path` + new `repo_path`), creation on a service subfolder, a `--writable-root` flag both sidecars must parse explicitly (sidecar-codex rejects unknown flags), scoping memory/discovery/orchestration on `servicePath`, frontend, docs. Seven decisions proposed (service identity, worktree-wide write boundary, worktree root as context — also needed so `acceptEdits` doesn't prompt on cross-package edits, detection, two layouts, `git log -1 -- <subtree>` staleness, ecosystem-relative service names); to be confirmed before Step 1. [phase-11-monorepo.md](phase-11-monorepo.md) |
| 2026-09-07 | **Phase 10 R8c landed (settings refactor)**: `SettingsService`'s ~22 hand-rolled getter/setter pairs and `SettingsController`'s two 22+-field DTOs collapsed to a typed `config/Settings` record + `config/SettingsPatch`, a private `Field<T>`-per-component table driving `SettingsService.current()` (cached, invalidated on write) and `apply(SettingsPatch)`, and `SettingsView` flattened via `@JsonUnwrapped` on the record (confirmed live that this project's Jackson 3 honors it on record accessors — no fallback needed). `SettingsService`'s public surface is now just `current()`/`apply()`/`systemProvider()`/`pricingFor()`/`setPricingFor()`; all ~15 external callers migrated from named getters to `settings.current().xxx()`. Wire JSON contract unchanged, so no frontend edits. Full write-up: [phase-10-review-followups.md](phase-10-review-followups.md)'s R8c bullet |
| 2026-09-07 | **Phase 10 R1 landed (provider seam)**: `Capabilities` (both `protocol.ts` copies) gained `unsupportedSessionFields`/`contextDirs`/`reportsCostUsd`; went with the sketch's option (a) — a committed `<package>/capabilities.json` per adapter, build-generated by a new `scripts/gen-provider-capabilities.mjs` postbuild step from the package's own `*_CAPABILITIES` const (not a mirrored `ProviderCatalog` constants class), read by a new backend `ProviderCatalog` **lazily per provider id** so a partially-built dev install still boots. `SidecarManager.buildArgs`/`SessionConfigFactory.prepare`/`SessionService.applyEstimatedCost` (renamed from `applyCodexCostEstimate`) now branch on `ProviderCapabilities`, zero provider-name checks; `SettingsService.codexPricing` generalized to `pricingFor(provider)` (key `<provider>.pricing`, `codex` unchanged, no migration). CI verifies `capabilities.json` freshness with a `git diff --exit-code` step after each package's build, not a `check-protocol-sync.mjs` extension as originally sketched — that script intentionally runs before `npm ci` for speed, before `dist/` exists to regenerate from. Full write-up: [phase-10-review-followups.md](phase-10-review-followups.md)'s R1 bullet |
| 2026-09-06 | **Phase 10 planned (post-review follow-ups)**: a fresh full read-through after Phase 9 Run F found the architecture sound — the backlog is second-order only. One real seam remains: three `"codex"` string branches in the backend (`SidecarManager.buildArgs`, `SessionConfigFactory.prepare`, `applyCodexCostEstimate`); fix by having each adapter **declare** its unsupported session fields and whether it reports USD in `Capabilities`, so a third provider needs zero backend edits (source-of-truth mechanism — build-generated `capabilities.json` vs. a mirrored `ProviderCatalog` — to be decided before that run). Plus: search paths must use `tryEmbed` so a failing Voyage key degrades to sparse instead of erroring; `AutoTitleService` is the third copy of G6's pattern and re-reads the whole journal per turn; two unbounded per-session maps; `SidecarHandle` leaks `this` from its constructor; `SessionController.list()` is N+1; FQN/`console.debug`/flat-settings nits. **Explicitly excluded**: splitting the large frontend dialogs — opportunistic only. Also planned as 10.5: **monorepo support** — today `repoPath` is both the worktree source and the service identity, so an ecosystem folder that *is* one git repo (services = `packages/*`) collapses to a single service. Proposed: introduce `servicePath` as the identity (`repoPath` demoted to git root; polyrepo is the `servicePath == repoPath` special case, so no data migration), cwd = the service subfolder inside the worktree, **whole worktree writable** (new `--writable-root` sidecar flag; Codex's existing `writableRoots`), worktree root as the ecosystem context, manifest-driven workspace detection (npm/pnpm/Maven/Cargo/go.work) with a glob-setting fallback, discovery staleness gated on `git log -1 -- <servicePath>` instead of `HEAD`. Six sub-decisions listed in the doc for confirmation before the run. [phase-10-review-followups.md](phase-10-review-followups.md) |
| 2026-08-29 | **Skill & agent library (Phase 6)**: curated library on top of the per-session sources. Embeddings via **Voyage API** (`CLAUDE_UI_VOYAGE_API_KEY`, `vector(1024)`, `voyage-3.5-lite`) behind an `EmbeddingClient` interface (shared groundwork for 5.3 RAG); feature degrades gracefully without the key. Import destinations **reuse skillsRoot** (+ new agents root), both promoted to persisted settings (env values stay defaults, like `ecosystem.root`). Scanner detection is **convention-first** (`SKILL.md` dir = skill, standalone agent `.md` = agent; name-contains heuristic only as low-confidence fallback). Remote sources via **`gh` (GitHub-only for now)** — `gh repo clone --depth 1` + `gh repo sync` into `.repo-cache` (SHA-256 slug): gh's stored auth makes private repos work with the same `gh auth login` prerequisite the PR features already have; a plain-git fallback for other remotes can slot in later. Sync notifications via dashboard polling + badge, not the per-session WS journal |
| 2026-09-11 | **Phase 11 Step 0 landed (skills-resolution spike, decision 8)**: re-verified the whole phase-11 spec against current code first (three parallel research passes) — design and nearly every file/line citation held; only cosmetic drift (a few stale line numbers, `GitWorktreeServiceTest` already exists so Step 1 extends rather than creates it, `ServiceDiscoveryController.ServiceView`'s git-identity field is actually named `path` not `repoPath`). Then ran the spike: `claude -p --setting-sources project` from a `packages/foo` cwd inside a throwaway `/tmp/mono` fixture listed **both** the package-level and git-root-level project skills — the spec's "decision A" outcome. So `AssetProvisioningService` materializes skills/agents at the **service cwd**, not the worktree root, and `excludeProvisionedAssets` writes the worktree-relative exclude path; Codex needs no change since it already derives its skills dir from `config.cwd`. Full output pasted into [phase-11-monorepo.md](phase-11-monorepo.md)'s decision 8. Steps 1–7 proceed per that doc. |
| 2026-09-11 | **Phase 11 Step 1 landed (`ServiceDetector` + `findServices`, pure)**: new `git/ServiceDetector` implements the manifest table (npm/yarn `package.json#workspaces`, `pnpm-workspace.yaml`, Maven `<modules>`, Gradle `settings.gradle(.kts)` `include(...)` incl. a trivial `project(...).projectDir` override, Cargo `[workspace] members`/`exclude`, `go.work` `use` lines/blocks, glob fallback filtered to manifest-bearing folders) with a shared minimal glob resolver (`*`/`**` = one dir level, `!` = exclusion); 13 fixture-tree tests. `GitWorktreeService` gained `ServiceInfo(name, servicePath, repoPath)`, `findServices` (layout (a)/(b)/polyrepo via the existing `findRepos`, submodule folders reported as their own repo), `repoRootOf`, `lastCommitTouching`; extended the *pre-existing* `GitWorktreeServiceTest` (it already covered `pickDefaultBranch`) with 8 real-`git init` cases. New setting `ecosystem.monorepo-service-globs` (default `packages/*,services/*,apps/*,libs/*`) end-to-end: `Settings`/`SettingsPatch`(+`Builder`)/`SettingsService` (+2 round-trip tests), frontend `Settings` type + a `SettingsDialog` input next to ecosystem root (`rest.ts`'s `updateSettings` Pick-union needed the new key added explicitly, an existing pattern, not a new one). No production caller yet, as designed. |
| 2026-09-11 | **Phase 11 Step 2 landed (`V14` + `SessionEntity`/`ServiceProfileRepository` `servicePath`)**: `V14__monorepo_service_path.sql` adds nullable `session.service_path` and renames `service_profile.repo_path` → `service_path` (UNIQUE follows) plus a new nullable `repo_path`. `SessionEntity` gains a `servicePath` record component whose default accessor is **overridden** to resolve NULL → `repoPath` (`@JsonProperty`-annotated — confirmed live, a plain non-record-component method is *not* auto-serialized by this Jackson 3 setup the way a record accessor is, so the spec's "for free" claim needed the explicit annotation it now has) plus a derived `cwdPath()` (worktree-relative subfolder), and a package-private `rawServicePath()` for `SessionRepository`'s insert (the only caller that needs the un-resolved nullable value). `ServiceProfileRepository`'s three existing callers (`ServiceDiscoveryService`/`Controller`/`McpTools`) were updated to keep reading the identity via the renamed `servicePath()` accessor — otherwise their `ServiceView`/tool-result "repoPath" fields would have silently started reading the new, currently-always-null `repoPath` column instead of the identity, a real behavior regression the rename would have introduced if left unfixed at this step (still no *scoping* change — that's Step 5). New `ServiceProfileRepositoryDbTest` (no pre-existing one to extend, unlike the spec's assumption) plus 3 new `SessionRepositoryDbTest` cases cover the null/non-null resolution and the Jackson serialization. Full suite green against a live V14 DB (`./mvnw test` incl. `integration`, frontend test+build). |
| 2026-09-11 | **Phase 11 Step 3 landed (session creation on a service subfolder)**: `SessionService.CreateOptions`/`SessionController.CreateSessionRequest` gain `servicePath`; `SessionConfigFactory.prepare` resolves it via a new `resolveRepoAndService` — `repoPath` from `GitWorktreeService.repoRootOf` when blank, else validated as that root, rejected unless it's the repo root itself or a `findServices`-known service under the session's resolved `ecosystemPath`. `configOverridesFrom` conditionally carries the *raw* servicePath (never the resolved one — a polyrepo source keeps emitting no key at all) so `duplicate`/`lastSessionConfig` reproduce it via the same config-blob path `ecosystemPath` already uses (no explicit top-level pass-through needed). `SessionService.create` now provisions assets at `entity.cwdPath()` (decision A) instead of always the worktree root, and `excludeProvisionedAssets` writes the matching worktree-relative exclude lines (byte-identical to before whenever `cwdPath == worktree`, i.e. every polyrepo session) — `AssetProvisioningService.provision`'s param renamed `worktree`→`assetsRoot` to match (pure rename). `MetaController.services()` moves onto `findServices`, `ServiceInfo` gains `repoPath`/`monorepo`, and the configured-default-repo fallback goes through the same detection (deduped on `repoPath`, not `path`, since a monorepo's packages never equal the repo root). **Bug found and fixed while wiring this up**: `GitWorktreeService.relativeName` (Step 1) returned `""` whenever the target equaled the ecosystem root — harmless in Step 1's own tests but would have produced a blank name in the picker for any plain (non-monorepo) repo pointed at directly as a layout-(a) ecosystem root, including the "configured default repo" fallback this step added; now falls back to the folder's own basename. 24 new/updated tests (`SessionConfigFactoryTest` ×4 new incl. a `GitWorktreeService`-faking helper, `SessionStateMachineTest`'s `FakeGitWorktreeService` grown + one real `create()`-driving test, new `MetaControllerTest` ×4). Full suite green. |
| 2026-09-11 | **Phase 11 Step 4 landed (sidecars: `--writable-root`, cwd, context dirs)**: both `sidecar/src/index.ts` and `sidecar-codex/src/index.ts` learn `--writable-root <dir>` explicitly (default = `--cwd`'s value), matching each package's existing "unknown flag is a hard error" parsing. `sidecar/src/permissions.ts`'s `readOnlyDenial` gains a 4th `writableRoot = cwd` param — the write-boundary check now uses it while relative-path resolution still uses `cwd`, and all 21 pre-existing tests (3-arg calls) keep passing unchanged, proving the default preserves polyrepo behavior byte-for-byte; 4 new tests cover the 4-arg monorepo case (sibling-package write allowed, original-checkout write denied). `sidecar-codex/src/session.ts`'s `sandboxPolicyFor` (now exported for testing, previously untested) populates `writableRoots: [config.writableRoot]` instead of the hardcoded `[]` — new `sidecar-codex/test/session.test.ts` (no prior test file for `session.ts` existed). Codex's `skills/extraRoots/set` needed no change (decision A already has it reading `config.cwd`, which the spec's "decision B" branch would have redirected instead). `SidecarManager.buildArgs` sends `--cwd s.cwdPath()` + unconditional `--writable-root s.worktreePath()`, and the `caps.contextDirs()` block now branches on `servicePath() != repoPath()`: polyrepo unchanged, layout (a) (ecosystem root *is* the repo) swaps the ecosystem context-dir for the worktree one (avoiding a stale duplicate of the very code being edited), layout (b) emits both. 4 new `SidecarManagerTest` cases (byte-for-byte polyrepo diff, layout (a), layout (b), a Codex-shaped `contextDirs:false` provider still getting `--writable-root` but no `--context-dir`). Full suite green across backend (`./mvnw test` incl. `integration`), both sidecars (`npm test` + `tsc`), `check-protocol-sync.mjs` (untouched — no protocol.ts/stdio.ts changes), and frontend. Live "sidecar pid … spawned (…)" log confirmation deferred to the manual test pass once Steps 5/6 land. |
| 2026-09-12 | **Phase 11 Step 5 landed (memory/discovery/orchestration scoped onto `servicePath`)**: every `session.repoPath()` used as a scope key (`SessionConfigFactory.memorySystemPromptBlock`'s `episodes.recentByService`, all five `session.repoPath()` sites in `ReflectionService`, `MemoryMcpTools.repoPathOf` → `servicePathOf`) becomes `session.servicePath()`; `MemoryController` already used `servicePath` throughout, and `MemoryPaths`' javadoc reworded (code unchanged). **New shared helper** `GitWorktreeService.isKnownService(ecosystemRoot, globs, repoRoot, servicePath)` — "is this the repo root, or one of `findServices`'s results" — extracted so `SessionConfigFactory` (Step 3) and the new `ServiceDiscoveryService`/`OrchestrationMcpTools` validation don't each hand-roll the same check; `SessionConfigFactory.resolveRepoAndService` now delegates to it. `ServiceDiscoveryRequested` is now 3-arg `(sessionId, servicePath, repoPath)`; `ServiceDiscoveryService` rewritten to resolve a `servicePath`'s repo root via `repoRootOf` + validate it's a known service (best-effort/silent on the close-triggered path, a thrown `IllegalArgumentException` on the manual rediscover/update path) and gate staleness on `GitWorktreeService.lastCommitTouching` (decision 6) instead of `rev-parse HEAD` — `GitCommandRunner` is no longer a direct dependency of this class at all. `ServiceDiscoveryController`'s `ServiceView.path` → `servicePath` (correction 4: the actual pre-existing field was `path`, not `repoPath` as the spec's prose said) plus its `services()` moves onto `findServices`; `ServiceDiscoveryMcpTools.visiblePaths` likewise. `OrchestrationMcpTools.listServices`/`spawnChildSession` move onto `findServices`/`repoRootOf`+`isKnownService` (gained a `SettingsService` dependency for the globs setting); `spawnChildSession` passes both `repoPath` (the resolved root) and the raw `servicePath` into `CreateOptions`; `checkChildren`/`reportResult` read `c.servicePath()` instead of `c.repoPath()`, so a child's reported "service" is the package name, not the repo's. Test fakes grew to support this: `FakeSessionRepository` gained `countChildren`/`findByParent`, `FakeEventJournal` gained `hasEventType`. New tests: `GitWorktreeServiceTest` ×3 (`isKnownService`), `MemoryMcpToolsTest` ×1 (servicePath vs repoPath scoping), `ServiceDiscoveryServiceTest` (new file, real `git init` fixtures — regenerate-vs-bump-only per package, reject-unknown-service ×2), `OrchestrationMcpToolsTest` (new file, real monorepo git fixture — child `repoPath` resolves to the monorepo root, `report_result` names the package). Full suite green (`./mvnw test` incl. `integration`, frontend) — no sidecar/protocol changes this step. |
| 2026-09-12 | **Phase 11 Step 6 landed (frontend)**: `protocol.ts`'s `ServiceInfo` gains `repoPath`/`monorepo`, `SessionEntity` gains `servicePath`/`cwdPath`, `SessionSummary` gains `servicePath`, `ServiceProfileView.path` → `servicePath` (`ServiceDiscoveryDialog`'s field-rename-only pass followed). `rest.ts`'s `serviceDiscoveryRediscover`/`serviceDiscoveryUpdate` send `servicePath` (matching the backend's Step 5 request-field rename); `createSession` stays untyped, callers decide the body. Both create dialogs now track the picker's selection as `servicePath` and derive `selectedService`/`selectedRepoPath` (`services[].find(s => s.path === servicePath)`) so `api.branches(...)` is called with the git root (not a package subfolder, which the backend would reject) and the create request carries both `repoPath` and `servicePath`; the picker's `<option>` text gets a plain `· monorepo` suffix for `s.monorepo` entries (a native `<select>` can't render a styled `.chip` inside an `<option>`, so this is the pragmatic equivalent) and the create dialog's ecosystem-field title gains the monorepo caveat. The three service-name chips (`SessionWidget`, `ExposeOverlay`, `ContinuationPickerDialog`) and the store's `SessionView` (gained a `servicePath` field alongside the existing `repoPath`, seeded the same way in `SessionWidget`'s REST-fetch effect) now show/carry `servicePath`, not `repoPath` — so a monorepo session's widget/Exposé-card/continuation-picker chip reads the package name, not the repo's. `GitPanel` and `useTicketImport` confirmed untouched, per the spec. `npm test` + `npm run build` green; no backend/sidecar files touched this step. |
| 2026-09-12 | **Phase 11 manual test pass — all green, run live against a real backend + browser (Playwright) before Step 7's docs**: built and ran the full app (`mvn package` + both sidecars' `npm run build`, since dist/ output — not just `tsc --noEmit` — is what actually ships); all 9 of the spec's manual-test-script items confirmed, several caught live in the exact wording the DoD asked for. Highlights: the picker showed `packages/foo`/`packages/bar` with the `· monorepo` suffix and the branch dropdown populated from the monorepo root; the live "sidecar pid … spawned (…)" log line showed `--cwd …/packages/foo --writable-root …/<id> --context-dir …/<id>` (worktree, correctly swapping for the ecosystem path per layout (a)); a cross-package `Write` under `acceptEdits` applied with no prompt while an `Edit` on the *original* `/tmp/mono` checkout was auto-denied naming the worktree, verbatim; `git log feat-x -1` in `/tmp/mono` showed the real commit; a reflected session's episode and an approved semantic-memory proposal landed at `~/claude-memory/services/foo/*.md` (not `services/mono/`); Rediscover produced two independent per-package profiles, and a commit touching only `packages/bar`'s README changed only its description text while `packages/foo`'s stayed byte-identical (SHA-unchanged bump, not a regeneration); `spawn_child_session` from a `packages/foo` session onto `packages/bar` created a real second worktree of `/tmp/mono` with `repoPath` resolved to the monorepo root and the child named `bar: <branch>`; layout (b) (`/tmp/eco/{mono,solo}`) logged both `--context-dir` values for the monorepo session and exactly the pre-phase single `--context-dir` for `solo`. The final regression pass used **real, non-fixture folders**: pointing the ecosystem root at `/mnt/d/projects` organically surfaced two genuine pre-existing Gradle multi-module projects (`kryptanalerts-frontend`, `winmon`) as real monorepos — `ServiceDetector`'s regex-based Gradle parsing correctly handled files with `enableFeaturePreview`/`pluginManagement`/`plugins {}` noise it was never tested against directly — and a session on a plain existing polyrepo (`agents`) produced a `git-path info/exclude` and sidecar spawn line identical to pre-phase-11 except for the added `--writable-root`. No regressions found; all cleanup sessions closed via REST afterward. |
| 2026-09-12 | **Phase 11 landed (monorepo support, Steps 0–7 all green)**: all eight confirmed decisions held through implementation with no reversals — (1) `servicePath` is the identity, `repoPath` demoted to "git root for worktree ops", polyrepo is the `servicePath == repoPath` special case and stayed byte-identical throughout (checked at every step, not just the end); (2) write boundary is the whole worktree, not the service subfolder, via a `--writable-root` flag both sidecars parse explicitly; (3) the session's own worktree root stands in as ecosystem context in monorepo mode (swapped for the ecosystem path in layout (a), added alongside it in layout (b)) so `acceptEdits` doesn't prompt on cross-package edits; (4) service detection is manifest-driven first (npm/yarn/pnpm/Maven/Gradle/Cargo/go.work) with a glob-fallback setting (`ecosystem.monorepo-service-globs`); (5) two layouts supported — ecosystem root *is* the monorepo, or contains one among polyrepo siblings; (6) discovery staleness gates on `git log -1 -- <subtree>` (`GitWorktreeService.lastCommitTouching`) instead of `rev-parse HEAD`, so an unrelated package's commit no longer forces a regeneration; (7) service names are ecosystem-root-relative paths; (8) skills/agents materialize at the service's own cwd, not the worktree root (Step 0's spike, confirmed live for both providers). One duplicated validation ("is this path a known service") was consolidated into a single `GitWorktreeService.isKnownService` shared by session creation, discovery, and orchestration rather than left to triplicate as the spec's step-by-step diffs would have produced. Full automated net added along the way (`ServiceDetectorTest`, extended `GitWorktreeServiceTest`, `SessionConfigFactoryTest`, `SidecarManagerTest`, sidecar/sidecar-codex vitest, `ServiceDiscoveryServiceTest`, `OrchestrationMcpToolsTest`, `MemoryMcpToolsTest`, frontend) plus the spec's full 9-step manual test script run live against real fixtures and a real existing polyrepo/monorepo folder — no regressions. Docs updated: `ARCHITECTURE.md` §2/§3d, `CLAUDE.md`'s ecosystem-root bullet, `DEPLOY.md` §7 + a new worktree-disk-cost note, this log. `grep -rn "findRepos" src/main/java` confirms only `GitWorktreeService` itself and `findServices`'s internal use remain. |
| 2026-09-13 | **Phase 12 planned (Linear cache, Serena MCP, context indicator)** — three independent tracks in one doc, confirmed with the user before writing: (1) Linear stays on the LLM+MCP route (the real deployment uses the OAuth toggle, so no backend credential exists for a direct GraphQL/MCP path; a backend-owned OAuth flow is the named next step, not this phase) — a 15-min in-memory cache for the assigned-tickets list and per-ref import results, `?refresh=true` + a `Refresh` button, and the system session pre-warmed + list prefetched when the create/quick dialog opens; (2) Serena is a **per-session opt-in, default off** (`serenaEnabled` on session + template, checkbox only when Settings → MCP servers → Serena root is set), layered at creation as a stdio entry `uv run --directory <root> serena start-mcp-server --context <claude-code|codex> --project <cwdPath> --open-web-dashboard false` (session's own `serena` key wins, like Linear/memory; context name from each provider's `capabilities.json`, no provider name in Java), Claude sessions additionally get Serena's own CC system-prompt override via the existing `extraSystemPrompt` seam, `MCP_TIMEOUT` set for the cold language-server start; (3) a provider-neutral `context_usage`/`context_compacted` event pair + `compact` command + `compact` capability in both sidecars (Claude from the SDK's `getContextUsage()`/`compact_boundary`, Codex from `tokenUsage.last`/`modelContextWindow` + `thread/compact` pending a spike), persisted on `session` (V15) and shown as a `ctx N%` chip with a Settings threshold (default 70 %) driving a one-shot **Compact-only** suggestion card — no hand-off action (7.3's continuation flow already covers that from the close dialog). |
| 2026-09-13 | **Phase 12 Track A landed (Linear ticket cache + pre-warm + picker UI, Steps A1–A3)**: new package-private `TicketCache` (single-flight `AtomicReference<CompletableFuture>` for the ticket list, a `ConcurrentHashMap` for per-ref import results, both TTL'd at 15 min via an injected `Clock` — test seam follows `ProviderCatalog`'s `@Autowired`-plus-package-private-constructor pattern) backs `TicketImportService.listMyTickets(boolean refresh)` (now returns `TicketList(tickets, fetchedAt, cached)`) and `importTicket` (cached under both the caller's uppercased input and the response's canonical `ticketRef`, so a URL and its short identifier hit the same entry; an OAuth-mode flip is treated as a cache miss rather than wiring a settings-change listener). `POST /api/tickets/recent` takes a `refresh` query flag; `GET /api/tickets/import/enabled` now also fire-and-forgets `SystemSessionService.warmUp()` (new: `tryAcquire`-guarded, never steals the lock from a live turn) and a background `listMyTickets(false)` prefetch, and returns a new `warm` flag (`SystemSessionService.isWarm()`) the two create dialogs use to pick honest "fetching from Linear…" vs. the 45s cold-start copy. `TicketPickerDialog` redesigned: header shows "as of N min ago" plus a `Refresh` icon button (`.pulse` while in flight, `r` hotkey, disabled during a fetch) and the ticket rows now stay visible through a refresh (error or success) instead of being blanked by the busy flag. 13 new/extended `TicketImportServiceTest` cases (TTL hit/miss, refresh bypass, single-flight under real concurrent threads, case-insensitive + URL-vs-ref import caching) plus the full existing suite (199 backend tests, `npm test` + `npm run build` in frontend) all green; no `sidecar`/`sidecar-codex` files touched by this track. |
| 2026-09-13 | **Phase 12 Track C landed (context-usage indicator + compact, Steps C0–C3)**: C0's live spike (docs/plan/phase-12-linear-cache-serena-context.md "Step C0 outcome") found the real Claude message sequence richer than assumed (a `status:'compacting'` then a `status`/`compact_result` pair before the re-`init` + `compact_boundary`, and confirmed manual compaction can fail per-model even when normal turns on that model work) and confirmed Codex's `thread/compact/start`/`thread/compacted` + `tokenUsage.last`/`modelContextWindow` shapes directly from the installed codex-cli's own protocol schema (`codex app-server generate-json-schema`) rather than a live run — both providers ship `compact: true`. Protocol v1 gained a `compact` command and `context_usage`/`context_compacted` events in both sidecar `protocol.ts` copies (`check-protocol-sync.mjs` green); Claude's `session.ts` calls `getContextUsage()` after every `turn_complete`/`context_compacted`, gated by a `turnInFlight` flag so a `compact` while a turn is running gets a non-fatal `error` instead of queuing; Codex derives `preTokens`/`postTokens` from its own before/after snapshot of `tokenUsage.last` (the `thread/compacted` notification itself carries none), queued through the same single-item FIFO as text turns so a compact can't race a `turn/start`. Backend: `V15__context_usage.sql` (`session.context_tokens`/`context_window`, nullable), `SessionService.compact(id)` refuses outside IDLE/PARKED (a PARKED session is woken via a `pendingCompactAfterWake` set drained by `becomeIdleAndDrainQueue`, ahead of any queued text turn) and otherwise sends the command directly, `POST /api/sessions/{id}/compact` (409 via the existing `IllegalStateException` handler), `ProviderCapabilities.compact` read from the handshake, new `session.context-warn-percent` setting (default 70, floor 30, ceiling 95, `SettingsService`'s generic `Field<T>` table). Frontend: `SessionWidget`'s `ctx N%` chip beside the cost chip (`--amber`/`--red` via the Settings threshold), `evaluateContextWarn`/`dismissContextWarn` store actions (armed/re-armed bookkeeping kept out of React so a crossing fires exactly once per arming period, independent of render timing — unit-tested directly), `ContextSuggestionCard` (styled like `PermissionCard`, `--warn-bg`), a `t-compacted` transcript divider. All suites green: 240 backend tests (incl. the Postgres-backed `SessionRepositoryDbTest` round trip and the new `SessionStateMachineTest`/`SettingsServiceTest` cases), `sidecar`/`sidecar-codex` vitest + `tsc`, frontend `vitest` (24 new store cases) + `tsc --noEmit` + `npm run build`. Track B (Serena) untouched by this work. |
| 2026-09-13 | **Phase 12 Track B landed (Serena MCP, Steps B0–B3)**: B0's spike found `uv` had only been installed on the Windows side of the WSL dev box (installed a native Linux `uv` for the backend to actually use) and that the checkout's `pyproject.toml` names the package `serena-agent`, not `serena` as the doc assumed — `SerenaService.validate` checks for the real name. New `mcp.serena-root`/`mcp.uv-path` settings (`SerenaService`: `configured()`/`root()`/`uvCommand()`, a stateless `validate(root, uvPath)` called from `SettingsController` before persisting so a bad root never lands in the DB, and a per-root-memoized `ccSystemPromptOverride()` that never throws — a failed capture just logs a WARN, since the prompt is an optimization, not a dependency). `V16__serena_enabled.sql` adds `session.serena_enabled` (V15 was already taken by Track C by the time this track started, so it's a separate migration, not the shared one the original doc draft assumed); `SessionEntity`/`SessionRepository` follow `reflectionEnabled`'s exact trail. `SessionConfigFactory.prepare()` rejects `serenaEnabled=true` with a configured-but-unset root as a 400, computes the session's cwd path *before* the entity exists (mirroring `SessionEntity.cwdPath()`'s own logic, since the MCP entry is baked into `mcpConfig` ahead of the builder call) and layers a `serena` stdio entry via the same `withDefaultServer` merge rule as Linear/memory (session's own `serena` key still wins); `--context` comes from a new `ProviderCapabilities.serenaContext` field (`"claude-code"`/`"codex"` in each sidecar's `capabilities.json`, regenerated via `gen-provider-capabilities.mjs` after adding the shared `Capabilities.serenaContext` field to both `protocol.ts` copies — `check-protocol-sync.mjs` green), never a hardcoded provider name — the Claude-only system-prompt override in `extraSystemPrompt` is likewise gated on `serenaContext === "claude-code"`, not a `"claude".equals(provider)` check. `SidecarManager` sets `MCP_TIMEOUT=300000` (a conservative, not live-measured, allowance for a cold JDT-LS-style download) ahead of the session's own `envVars` so it can still override. Frontend: a "Serena" checkbox in `CreateSessionDialog` (hidden unless `settings.mcpSerenaRoot` is set), a `serena` chip in `SessionWidget`, and a new "MCP servers" Settings section (two text inputs, a 400's `detail` surfaced inline like `codexPricing`'s error handling) — `TemplateManager` and `QuickSessionDialog`/`DuplicateDialog` needed no new UI at all: `serenaEnabled` round-trips through the same generic "advanced JSON" template field and `configOverridesFrom`/`lastSessionConfig` paths `reflectionEnabled` already used, since neither of those ever got a dedicated template checkbox either. 23 new/extended `SessionConfigFactoryTest` cases, a new `SerenaServiceTest` (11 cases, hand-written process-runner fake, no mocking framework), a `SessionRepositoryDbTest` round-trip case, plus every existing `ProviderCapabilities`/`SessionConfigFactory`/`Settings` construction site across 9 test files updated for the two new trailing fields. Full suite green: 262 backend tests (`./mvnw clean verify` against live Postgres, migrations 1–16), `sidecar`/`sidecar-codex` vitest + `tsc` + protocol-sync, frontend `vitest` + `tsc --noEmit` + `npm run build`. Live end-to-end Serena tool use (a real session calling `find_symbol`) was not exercised in this pass — B0's standalone spike confirmed the server starts and waits on its stdio handshake correctly, but a full agent-driven run is still open per the phase's manual test script items 5–8. |
| 2026-09-15 | **Phase 11 follow-up: monorepo workspace detection made opt-in, off by default** — real ecosystem use surfaced false positives (a repo like `auth-gateway` declares `package.json#workspaces` only to publish a couple of API-client sub-packages, not because it's a monorepo this app should split into several services), and it was getting auto-split anyway. New setting `ecosystem.monorepo-detection-enabled` (boolean, default `false`, Settings dialog → "Sessions" → "Monorepo detection", one global toggle for every repo under the ecosystem root — a per-repo override was considered and explicitly rejected in favor of simplicity) gates `ServiceDetector.detect` inside `GitWorktreeService.addServicesForRepo`; off, every repo (or the ecosystem root itself in layout (a)) is one service regardless of any workspace manifest — on, behavior is exactly Phase 11's original design. `findServices`/`isKnownService` both gained the flag as a new parameter, threaded through all 6 call sites (`MetaController`, `SessionConfigFactory`, `ServiceDiscoveryService`/`Controller`/`McpTools`, `OrchestrationMcpTools`). `GitWorktreeServiceTest` gained 2 cases (detection-disabled yields one service despite a workspace manifest; `isKnownService` rejects a package when disabled), `SettingsServiceTest` gained the usual default/round-trip pair; every other positional `new Settings(...)` test call site (8 of them) took the new field as `true` to keep pre-existing detection-reliant scenarios passing unchanged. Full suite green: 267 backend tests (`mvn test` against live Postgres), frontend `vitest` + `tsc --noEmit` + `npm run build`. Docs updated: `CLAUDE.md`'s ecosystem-root bullet, `DEPLOY.md` §7, `phase-11-monorepo.md` decision 4, this log. |
| 2026-09-15 | **Phase 13 planned (graphify knowledge-graph MCP, one-of with Serena)** — preceded by a security review of the graphify checkout (`/mnt/d/projects/graphify`, v0.9.62 `c7ec108`): verdict *good for a backend-driven integration* (no telemetry, no network during a build, no `shell=True`/`eval`/`pickle`, a solid `security.py` SSRF/size/sanitisation layer, pinned `uv.lock`, bandit + pip-audit, a long record of security fixes), with a deliberate posture: **never** its `/graphify` skill (auto-`pip install`s from PyPI inside the agent session and executes a repo-local `.graphify_python`), **never** `graphify install`/`hook install` (rewrite `~/.claude/settings.json`, `~/.claude/CLAUDE.md`, git hooks), **always** AST-only (`graphify update`; without `--code-only` the semantic pass ships doc contents to whichever API key is in the env), no `graph.html` (loads vis-network from unpkg). Four decisions confirmed with the user: (1) **global one-of** — a persisted `mcp.code-intel ∈ {none, serena, graphify}` selector, sessions get one on/off flag for the selected tool (per-session one-of and "both" declined: two processes + two competing "use me first" prompts per session); (2) **per-session graph, built asynchronously right after the worktree exists, refreshed after every completed turn (coalesced), stored outside the worktree** at `<worktree-root>/.graphify/<id>` (shared per-service and build-once declined); (3) **local checkout via `uv`** mirroring Serena (`mcp.graphify-root` + `mcp.uv-path`; PyPI declined — the reviewed code is the code that runs); (4) **code-only** (docs via `--backend claude-cli` is the named follow-up). Spike confirmed live: `uv run --directory <root> --no-dev --extra mcp --extra sql graphify update <path>` builds from empty (this repo: 2572 nodes / 7685 edges, ~80 s on DrvFS at ~5 s CPU, `.gitignore` honoured, worktree untouched with `GRAPHIFY_OUT` absolute), `GRAPHIFY_VIZ_NODE_LIMIT=0` suppresses HTML, the stdio MCP server inits in 0.8 s, hot-reloads `graph.json` on mtime change and tolerates a graph that appears after it started, and the rebuild lock is per output dir. Also confirmed on review of the doc: `session.serena_enabled` generalizes to `code_intel TEXT` (V17, config key `codeIntelEnabled` with `serenaEnabled` as legacy alias); single-flight + coalesced post-turn refresh without change detection; scope = `cwdPath`; selector defaults to `serena` when a Serena root exists. Still proposals to confirm per step: one command for build and refresh; graph dir under `<worktree-root>/.graphify/`; our own provider-neutral prompt block for both providers; in-memory + journaled `code_intel_status` (no column); 180 s validation timeout for the first env sync. See docs/plan/phase-13-graphify.md. |
| 2026-09-15 | **Phase 13 landed (graphify, Steps 1–4)**: all five open proposals (6, 7, 10, 11, 13) confirmed as written before Step 1. What changed on contact with the code: (a) `SettingsService` resolves the decision-12 default inside `current()` (a `Field` default can't see another key) and exposes `storedCodeIntel()` so `SettingsController` can tell an *implicit* `serena` (computed from the root) from an explicit one — blanking the Serena root on an install that never touched the selector just flips the default to `none`, whereas blanking the root of an explicitly selected tool is the planned 400 "select none first"; selector validity + root rules live in a static `SettingsService.validateCodeIntel` (unit-tested; the controller has no test class). (b) `GraphifyService`'s validation runner drains stdout/stderr on virtual threads *before* `waitFor`, unlike `SerenaService`'s, so the 180 s budget really fires mid-sync. (c) `SessionSummary` did **not** gain `codeIntel` — the widget chip reads the full entity, which is what carried `serenaEnabled` before; `TemplateManager`/`DuplicateDialog` needed no change (the flag rides the generic overrides path as before). (d) The build pipeline is a `BuildLauncher`/`BuildProcess` seam over `ProcessBuilder` (not the `ProcessRunner` used for `--version`, which can't be killed), `graphify update`'s output parsed for `[graphify watch] Rebuilt: N nodes, M edges` (absent on a no-change run — counts carry over) and, on exit ≠ 0, the log tail's `Rebuild failed: …`/`error` line ahead of the generic "Nothing to update or rebuild failed" trailer; `SessionService` takes `GraphifyService` as a nullable last constructor arg (null in every existing unit test, like `autoTitleService`). (e) `MaintenanceController.orphans()` was listing the `_system/` container as an orphan worktree (exact-path compare against `_system/<id>`) — a pre-existing false positive that `clean()` happily deleted during this phase's manual test; now excluded by name. Live on this repo: graph built in **10.8 s** from empty and **6.6 s** on refresh — the ~80 s Step-0 figure was `/mnt/d` (DrvFS) latency, worktrees live on ext4; the agent answered "which classes call SidecarManager.spawn" through `query_graph`/`get_neighbors`/`get_node` with the worktree staying clean, its own added method was found by `get_node` after the following turn, close removed `.graphify/<id>`, `orphans/clean` swept a stray dir, and a Codex session got the same entry + prompt block + build (its tool round trip not exercised: `codex` on this WSL box lacks `bubblewrap`, a pre-existing environment gap). Coalescing (manual test item 5) is pinned by a deterministic unit test rather than a timing-dependent live run. Suites: 308 backend unit tests (+22), `SessionRepositoryDbTest` green against the compose DB with V17, frontend vitest 40 (+3) + `tsc` + `npm run build`. Docs: CLAUDE.md, ARCHITECTURE.md §3g, DEPLOY.md §8c, PROTOCOL.md `code_intel_status`. |

## Repository layout (target)

```
claude-ui/
├─ pom.xml, src/…          # Spring Boot backend (existing skeleton)
├─ sidecar/                # Node + TS session engine (@anthropic-ai/claude-agent-sdk)
├─ frontend/               # Vite + React + TS dashboard
├─ docker-compose.yaml     # PostgreSQL (pgvector image)
└─ docs/
   ├─ plan/                # These phase documents
   └─ PROTOCOL.md          # Authoritative message contracts (written in Phase 1–2)
```

## Phases

| Phase | Doc | Outcome |
|---|---|---|
| 0 | [phase-0-foundation.md](phase-0-foundation.md) | Toolchain, git repo, Postgres up, app boots against it |
| 1 | [phase-1-sidecar.md](phase-1-sidecar.md) | Standalone session engine proven from the terminal |
| 2 | [phase-2-backend.md](phase-2-backend.md) | REST + WS backend: worktrees, process supervision, journal, templates |
| 3 | [phase-3-frontend.md](phase-3-frontend.md) | Dashboard with live widgets, approvals, reconnect/replay |
| 4 | [phase-4-hardening.md](phase-4-hardening.md) | Lifecycle safety, limits, crash/resume, cost display |
| 5 | [phase-5-extensions.md](phase-5-extensions.md) | Backlog: git panel, PRs, RAG/memory on pgvector, … |
| 6 | [phase-6-skill-library.md](phase-6-skill-library.md) | Skill & agent library: scan/import/tag/search (pgvector), scheduled source sync |
| 7 | [phase-7-ux-and-orchestration.md](phase-7-ux-and-orchestration.md) | Hotkeys, window management (maximize/minimize/Exposé), session handoff, multi-service parent/child fan-out |
| 8 | [phase-8-service-discovery.md](phase-8-service-discovery.md) | Ecosystem service discovery: auto-generated per-service description + embedding, `service_description`/`find_service` MCP tools |
| 9 | [phase-9-production-hardening.md](phase-9-production-hardening.md) | Architectural review backlog: tests + CI, provider decoupling of system/LLM features, dedup, `SessionService` split, doc refresh |
| 10 | [phase-10-review-followups.md](phase-10-review-followups.md) | Post-review follow-ups: the last provider-name seam (capabilities-declared unsupported fields), search degrading gracefully without Voyage, small races/leaks, `list()` N+1, cleanup nits, typed settings record |
| 11 | [phase-11-monorepo.md](phase-11-monorepo.md) | Monorepo support: a service is a folder inside a git repo (`servicePath` as identity, `repoPath` demoted to git root), cwd = the package subfolder, whole worktree writable, manifest-driven workspace detection, per-subtree discovery staleness |
| 12 | [phase-12-linear-cache-serena-context.md](phase-12-linear-cache-serena-context.md) | Linear ticket list/import cache (15 min, refresh, system-session pre-warm + prefetch); Serena stdio MCP as a per-session opt-in from a local checkout; per-session context-usage chip with a compact-in-place suggestion |
| 13 | [phase-13-graphify.md](phase-13-graphify.md) | Graphify knowledge-graph MCP as the second code-intelligence tool: global one-of selector (`none`/Serena/Graphify), per-session code-only graph built asynchronously outside the worktree and refreshed after every turn, driven from a reviewed local checkout via `uv` |
| 14 | [phase-14-codegraph.md](phase-14-codegraph.md) | CodeGraph MCP as the third code-intelligence tool in the same one-of selector: per-session index built synchronously during PROVISIONING inside the worktree (git-excluded), kept fresh by the server's own watcher, driven from a reviewed local checkout on the backend's Node with telemetry/update-check/daemon off |

Each phase ends in a runnable state, has a **Definition of Done** checklist, and a
**manual test script** with concrete commands and expected results. A phase is not
started until the previous phase's DoD is fully checked.

## Cross-cutting conventions

- **Ports**: backend `8080`, Vite dev server `5173`, Postgres `5432`.
- **Package root**: `de.pamir.claude.ui`.
- **All inter-process protocols are NDJSON** (one JSON object per line): sidecar stdio and the WS channel.
- **Event ordering**: every session event gets a per-session monotonic `seq` (assigned by the backend). Clients resume with `afterSeq`.
- **Sidecar logging** goes to stderr only; stdout is protocol-pure.
- **Errors over REST** use RFC 7807 problem+json (Spring's default `ProblemDetail`).
- **Secrets/config**: auth token and DB credentials come from `application-local.yaml` (gitignored) or env vars, never committed.
